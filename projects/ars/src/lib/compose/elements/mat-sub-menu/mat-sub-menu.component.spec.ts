/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MatSubMenuComponent } from './mat-sub-menu.component';

describe('MatSubMenuComponent', () => {
  let component: MatSubMenuComponent;
  let fixture: ComponentFixture<MatSubMenuComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MatSubMenuComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MatSubMenuComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
