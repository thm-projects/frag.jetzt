variables:
  BUILD_DIR: dist
  ANDROID_BASE64_KEY: "Tm90IHZhbGlkIHRlc3Rpbmcga2V5" # Not valid testing key
  CORDOVA_BUILD_JSON: "notPresent.json"

stages:
  - test

test:
  stage: test
  tags:
    - docker-compose
  allow_failure: true
  script:
    - alias docker-compose='docker compose'
    - docker-compose
