import { M3DynamicThemePortalDirective } from './m3-dynamic-theme-portal.directive';

describe('M3DynamicThemePortalDirective', () => {
  it('should create an instance', () => {
    const directive = new M3DynamicThemePortalDirective();
    expect(directive).toBeTruthy();
  });
});
