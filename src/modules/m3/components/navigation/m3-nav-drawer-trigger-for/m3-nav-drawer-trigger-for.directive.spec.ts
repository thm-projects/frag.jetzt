import { M3NavDrawerTriggerForDirective } from './m3-nav-drawer-trigger-for.directive';

describe('M3NavDrawerTriggerForDirective', () => {
  it('should create an instance', () => {
    const directive = new M3NavDrawerTriggerForDirective();
    expect(directive).toBeTruthy();
  });
});
