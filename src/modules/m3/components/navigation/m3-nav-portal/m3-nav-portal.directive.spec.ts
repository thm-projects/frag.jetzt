import { M3NavPortalDirective } from './m3-nav-portal.directive';

describe('M3NavPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new M3NavPortalDirective();
    expect(directive).toBeTruthy();
  });
});
