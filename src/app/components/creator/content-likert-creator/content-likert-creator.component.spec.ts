/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ContentLikertCreatorComponent } from './content-likert-creator.component';

describe('ContentLikertCreatorComponent', () => {
  let component: ContentLikertCreatorComponent;
  let fixture: ComponentFixture<ContentLikertCreatorComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ContentLikertCreatorComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ContentLikertCreatorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
