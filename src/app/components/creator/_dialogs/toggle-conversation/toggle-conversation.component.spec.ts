/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ToggleConversationComponent } from './toggle-conversation.component';

describe('ToggleConversationComponent', () => {
  let component: ToggleConversationComponent;
  let fixture: ComponentFixture<ToggleConversationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ToggleConversationComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ToggleConversationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
