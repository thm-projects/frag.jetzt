/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ModeratorRefreshCodeComponent } from './moderator-refresh-code.component';

describe('ModeratorRefreshCodeComponent', () => {
  let component: ModeratorRefreshCodeComponent;
  let fixture: ComponentFixture<ModeratorRefreshCodeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ModeratorRefreshCodeComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ModeratorRefreshCodeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
