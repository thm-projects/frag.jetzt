/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DeleteModerationCommentsComponent } from './delete-moderation-comments.component';

describe('DeleteModerationCommentsComponent', () => {
  let component: DeleteModerationCommentsComponent;
  let fixture: ComponentFixture<DeleteModerationCommentsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ DeleteModerationCommentsComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DeleteModerationCommentsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
