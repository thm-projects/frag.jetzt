/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ModeratorJoinComponent } from './moderator-join.component';

describe('ModeratorJoinComponent', () => {
  let component: ModeratorJoinComponent;
  let fixture: ComponentFixture<ModeratorJoinComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ModeratorJoinComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ModeratorJoinComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
