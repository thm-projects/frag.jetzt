/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AskOnboardingComponent } from './ask-onboarding.component';

describe('AskOnboardingComponent', () => {
  let component: AskOnboardingComponent;
  let fixture: ComponentFixture<AskOnboardingComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AskOnboardingComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AskOnboardingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
