/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UpdateInfoDialogComponent } from './update-info-dialog.component';

describe('UpdateInfoDialogComponent', () => {
  let component: UpdateInfoDialogComponent;
  let fixture: ComponentFixture<UpdateInfoDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ UpdateInfoDialogComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(UpdateInfoDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
