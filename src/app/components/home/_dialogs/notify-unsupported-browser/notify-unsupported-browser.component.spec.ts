/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NotifyUnsupportedBrowserComponent } from './notify-unsupported-browser.component';

describe('NotifyUnsupportedBrowserComponent', () => {
  let component: NotifyUnsupportedBrowserComponent;
  let fixture: ComponentFixture<NotifyUnsupportedBrowserComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NotifyUnsupportedBrowserComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(NotifyUnsupportedBrowserComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
