import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
    // Your declarations (excluding standalone components)
  ],
  imports: [
    CommonModule,
    // Other imports (excluding standalone components)
  ],
  exports: [
    // Your exports
  ],
})
export class HomePageModule {}
