/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CommentResponseViewComponent } from './comment-response-view.component';

describe('CommentResponseViewComponent', () => {
  let component: CommentResponseViewComponent;
  let fixture: ComponentFixture<CommentResponseViewComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CommentResponseViewComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CommentResponseViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
