/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UIRegistrationComponent } from './uiregistration.component';

describe('UIRegistrationComponent', () => {
  let component: UIRegistrationComponent;
  let fixture: ComponentFixture<UIRegistrationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ UIRegistrationComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(UIRegistrationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
