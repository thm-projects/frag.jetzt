/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GlobalStatusIndicatorComponent } from './global-status-indicator.component';

describe('GlobalStatusIndicatorComponent', () => {
  let component: GlobalStatusIndicatorComponent;
  let fixture: ComponentFixture<GlobalStatusIndicatorComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GlobalStatusIndicatorComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GlobalStatusIndicatorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
