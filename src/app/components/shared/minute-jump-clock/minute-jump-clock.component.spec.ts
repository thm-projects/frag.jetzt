/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MinuteJumpClockComponent } from './minute-jump-clock.component';

describe('MinuteJumpClockComponent', () => {
  let component: MinuteJumpClockComponent;
  let fixture: ComponentFixture<MinuteJumpClockComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MinuteJumpClockComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MinuteJumpClockComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
