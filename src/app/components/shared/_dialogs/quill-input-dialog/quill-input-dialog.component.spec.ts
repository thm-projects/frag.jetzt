/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { QuillInputDialogComponent } from './quill-input-dialog.component';

describe('QuillInputDialogComponent', () => {
  let component: QuillInputDialogComponent;
  let fixture: ComponentFixture<QuillInputDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ QuillInputDialogComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(QuillInputDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
