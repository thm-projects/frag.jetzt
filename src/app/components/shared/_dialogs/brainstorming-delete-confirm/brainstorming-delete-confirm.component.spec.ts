/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BrainstormingDeleteConfirmComponent } from './brainstorming-delete-confirm.component';

describe('BrainstormingDeleteConfirmComponent', () => {
  let component: BrainstormingDeleteConfirmComponent;
  let fixture: ComponentFixture<BrainstormingDeleteConfirmComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ BrainstormingDeleteConfirmComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(BrainstormingDeleteConfirmComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
