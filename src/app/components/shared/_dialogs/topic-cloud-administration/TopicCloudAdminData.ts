export interface TopicCloudAdminData{
    blackList: string[];
    considerVotes: boolean;
    profanityFilter: boolean;
    hideBlacklist: boolean;
    // constructor(private bList: string[], private cVotes: boolean,
    //             private pFilter: boolean, private hBlacklist: boolean){
    //     this.blackList = bList;
    //     this.considerVotes = cVotes;
    //     this.profanityFilter = pFilter;
    //     this.hideBlacklist = hBlacklist;
    // }
}