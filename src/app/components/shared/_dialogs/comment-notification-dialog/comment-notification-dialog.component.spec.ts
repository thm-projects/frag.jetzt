/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CommentNotificationDialogComponent } from './comment-notification-dialog.component';

describe('CommentNotificationDialogComponent', () => {
  let component: CommentNotificationDialogComponent;
  let fixture: ComponentFixture<CommentNotificationDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CommentNotificationDialogComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CommentNotificationDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
