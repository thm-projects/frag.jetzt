/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GptRoomSettingsComponent } from './gpt-room-settings.component';

describe('GptRoomSettingsComponent', () => {
  let component: GptRoomSettingsComponent;
  let fixture: ComponentFixture<GptRoomSettingsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GptRoomSettingsComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GptRoomSettingsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
