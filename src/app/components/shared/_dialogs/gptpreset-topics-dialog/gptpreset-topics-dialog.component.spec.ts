/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GPTPresetTopicsDialogComponent } from './gptpreset-topics-dialog.component';

describe('GPTPresetTopicsDialogComponent', () => {
  let component: GPTPresetTopicsDialogComponent;
  let fixture: ComponentFixture<GPTPresetTopicsDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [GPTPresetTopicsDialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(GPTPresetTopicsDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
