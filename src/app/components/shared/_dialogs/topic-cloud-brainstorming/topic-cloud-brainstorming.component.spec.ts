/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TopicCloudBrainstormingComponent } from './topic-cloud-brainstorming.component';

describe('TopicCloudBrainstormingComponent', () => {
  let component: TopicCloudBrainstormingComponent;
  let fixture: ComponentFixture<TopicCloudBrainstormingComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TopicCloudBrainstormingComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TopicCloudBrainstormingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
