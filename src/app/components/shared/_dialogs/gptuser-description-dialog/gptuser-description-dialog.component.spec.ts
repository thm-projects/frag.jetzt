/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GPTUserDescriptionDialogComponent } from './gptuser-description-dialog.component';

describe('GPTUserDescriptionDialogComponent', () => {
  let component: GPTUserDescriptionDialogComponent;
  let fixture: ComponentFixture<GPTUserDescriptionDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GPTUserDescriptionDialogComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GPTUserDescriptionDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
