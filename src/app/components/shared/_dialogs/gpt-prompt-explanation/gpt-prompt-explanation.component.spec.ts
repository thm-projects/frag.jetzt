/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GptPromptExplanationComponent } from './gpt-prompt-explanation.component';

describe('GptPromptExplanationComponent', () => {
  let component: GptPromptExplanationComponent;
  let fixture: ComponentFixture<GptPromptExplanationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [GptPromptExplanationComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(GptPromptExplanationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
