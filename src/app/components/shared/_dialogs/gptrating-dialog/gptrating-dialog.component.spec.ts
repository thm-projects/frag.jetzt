/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GPTRatingDialogComponent } from './gptrating-dialog.component';

describe('GPTRatingDialogComponent', () => {
  let component: GPTRatingDialogComponent;
  let fixture: ComponentFixture<GPTRatingDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GPTRatingDialogComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GPTRatingDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
