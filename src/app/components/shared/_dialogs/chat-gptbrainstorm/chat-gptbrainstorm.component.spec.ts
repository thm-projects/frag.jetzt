/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ChatGPTBrainstormComponent } from './chat-gptbrainstorm.component';

describe('ChatGPTBrainstormComponent', () => {
  let component: ChatGPTBrainstormComponent;
  let fixture: ComponentFixture<ChatGPTBrainstormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ChatGPTBrainstormComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ChatGPTBrainstormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
