/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiLevelQuotaInputComponent } from './multi-level-quota-input.component';

describe('MultiLevelQuotaInputComponent', () => {
  let component: MultiLevelQuotaInputComponent;
  let fixture: ComponentFixture<MultiLevelQuotaInputComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MultiLevelQuotaInputComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MultiLevelQuotaInputComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
