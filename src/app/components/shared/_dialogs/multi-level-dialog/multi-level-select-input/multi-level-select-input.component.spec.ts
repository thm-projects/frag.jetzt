/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiLevelSelectInputComponent } from './multi-level-select-input.component';

describe('MultiLevelSelectInputComponent', () => {
  let component: MultiLevelSelectInputComponent;
  let fixture: ComponentFixture<MultiLevelSelectInputComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MultiLevelSelectInputComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MultiLevelSelectInputComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/