/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiLevelSelectActionComponent } from './multi-level-select-action.component';

describe('MultiLevelSelectActionComponent', () => {
  let component: MultiLevelSelectActionComponent;
  let fixture: ComponentFixture<MultiLevelSelectActionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MultiLevelSelectActionComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MultiLevelSelectActionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
