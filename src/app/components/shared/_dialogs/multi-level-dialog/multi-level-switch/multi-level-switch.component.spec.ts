/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiLevelSwitchComponent } from './multi-level-switch.component';

describe('MultiLevelSwitchComponent', () => {
  let component: MultiLevelSwitchComponent;
  let fixture: ComponentFixture<MultiLevelSwitchComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MultiLevelSwitchComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MultiLevelSwitchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
