/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiLevelInputActionComponent } from './multi-level-input-action.component';

describe('MultiLevelInputActionComponent', () => {
  let component: MultiLevelInputActionComponent;
  let fixture: ComponentFixture<MultiLevelInputActionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MultiLevelInputActionComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MultiLevelInputActionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
