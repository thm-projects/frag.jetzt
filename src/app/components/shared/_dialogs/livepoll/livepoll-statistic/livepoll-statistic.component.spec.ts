/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LivepollStatisticComponent } from './livepoll-statistic.component';

describe('LivepollStatisticComponent', () => {
  let component: LivepollStatisticComponent;
  let fixture: ComponentFixture<LivepollStatisticComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [LivepollStatisticComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(LivepollStatisticComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
