/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LivepollConfirmationDialogComponent } from './livepoll-confirmation-dialog.component';

describe('LivepollConfirmationDialogComponent', () => {
  let component: LivepollConfirmationDialogComponent;
  let fixture: ComponentFixture<LivepollConfirmationDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [LivepollConfirmationDialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(LivepollConfirmationDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
