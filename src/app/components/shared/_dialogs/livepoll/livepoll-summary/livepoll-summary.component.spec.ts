/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LivepollSummaryComponent } from './livepoll-summary.component';

describe('LivepollSummaryComponent', () => {
  let component: LivepollSummaryComponent;
  let fixture: ComponentFixture<LivepollSummaryComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [LivepollSummaryComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(LivepollSummaryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
