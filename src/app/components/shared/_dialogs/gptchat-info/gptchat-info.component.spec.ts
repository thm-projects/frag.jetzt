/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GPTChatInfoComponent } from './gptchat-info.component';

describe('GPTChatInfoComponent', () => {
  let component: GPTChatInfoComponent;
  let fixture: ComponentFixture<GPTChatInfoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GPTChatInfoComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GPTChatInfoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
