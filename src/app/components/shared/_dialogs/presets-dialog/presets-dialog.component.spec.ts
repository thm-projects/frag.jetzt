/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PresetsDialogComponent } from './presets-dialog.component';

describe('PresetsDialogComponent', () => {
  let component: PresetsDialogComponent;
  let fixture: ComponentFixture<PresetsDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [PresetsDialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(PresetsDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
