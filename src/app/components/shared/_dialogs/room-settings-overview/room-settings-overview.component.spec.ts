/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RoomSettingsOverviewComponent } from './room-settings-overview.component';

describe('RoomSettingsOverviewComponent', () => {
  let component: RoomSettingsOverviewComponent;
  let fixture: ComponentFixture<RoomSettingsOverviewComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RoomSettingsOverviewComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(RoomSettingsOverviewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
