/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ChatGPTPromptPresetComponent } from './chat-gptprompt-preset.component';

describe('ChatGPTPromptPresetComponent', () => {
  let component: ChatGPTPromptPresetComponent;
  let fixture: ComponentFixture<ChatGPTPromptPresetComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ChatGPTPromptPresetComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ChatGPTPromptPresetComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
