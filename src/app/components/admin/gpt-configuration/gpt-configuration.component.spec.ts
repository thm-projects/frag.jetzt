/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GptConfigurationComponent } from './gpt-configuration.component';

describe('GptConfigurationComponent', () => {
  let component: GptConfigurationComponent;
  let fixture: ComponentFixture<GptConfigurationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GptConfigurationComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GptConfigurationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
