/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { KeycloakProviderComponent } from './keycloak-provider.component';

describe('KeycloakProviderComponent', () => {
  let component: KeycloakProviderComponent;
  let fixture: ComponentFixture<KeycloakProviderComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ KeycloakProviderComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(KeycloakProviderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
