/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateMotdComponent } from './create-motd.component';

describe('CreateMotdComponent', () => {
  let component: CreateMotdComponent;
  let fixture: ComponentFixture<CreateMotdComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CreateMotdComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CreateMotdComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
