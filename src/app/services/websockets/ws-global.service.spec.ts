/*import { TestBed } from '@angular/core/testing';

import { WsGlobalService } from './ws-global.service';

describe('WsGlobalService', () => {
  let service: WsGlobalService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(WsGlobalService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
