/*import { TestBed } from '@angular/core/testing';

import { WsLivepollService } from './ws-livepoll.service';

describe('WsLivepollService', () => {
  let service: WsLivepollService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(WsLivepollService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
