/*import { TestBed } from '@angular/core/testing';

import { WsCommentChangeService } from './ws-comment-change.service';

describe('WsCommentChangeService', () => {
  let service: WsCommentChangeService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(WsCommentChangeService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
