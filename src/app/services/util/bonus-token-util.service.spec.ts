/*import { TestBed } from '@angular/core/testing';

import { BonusTokenUtilService } from './bonus-token-util.service';

describe('BonusTokenUtilService', () => {
  let service: BonusTokenUtilService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(BonusTokenUtilService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
