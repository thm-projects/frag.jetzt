/*import { TestBed } from '@angular/core/testing';

import { GptEncoderService } from './gpt-encoder.service';

describe('GptEncoderService', () => {
  let service: GptEncoderService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GptEncoderService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
