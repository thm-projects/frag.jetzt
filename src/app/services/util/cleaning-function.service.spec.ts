/*
import { TestBed } from '@angular/core/testing';

import { CleaningFunctionService } from './cleaning-function.service';

describe('CleaningFunctionService', () => {
  let service: CleaningFunctionService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CleaningFunctionService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
