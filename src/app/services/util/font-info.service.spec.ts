/*import { TestBed } from '@angular/core/testing';

import { FontInfoService } from './font-info.service';

describe('FontInfoService', () => {
  let service: FontInfoService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FontInfoService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
