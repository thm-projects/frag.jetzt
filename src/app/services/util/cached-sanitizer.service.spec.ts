/*import { TestBed } from '@angular/core/testing';

import { CachedSanitizerService } from './cached-sanitizer.service';

describe('CachedSanitizerService', () => {
  let service: CachedSanitizerService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CachedSanitizerService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
