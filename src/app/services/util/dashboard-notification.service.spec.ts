/*import { TestBed, async, inject } from '@angular/core/testing';
import { DashboardNotificationService } from './dashboard-notification.service';

describe('Service: DashboardNotification', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DashboardNotificationService]
    });
  });

  it('should ...', inject([DashboardNotificationService], (service: DashboardNotificationService) => {
    expect(service).toBeTruthy();
  }));
});
 */
