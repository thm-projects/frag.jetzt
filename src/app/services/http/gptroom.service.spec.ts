/*import { TestBed } from '@angular/core/testing';

import { GPTRoomService } from './gptroom.service';

describe('GPTRoomService', () => {
  let service: GPTRoomService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GPTRoomService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
