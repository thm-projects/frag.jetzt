/*import { TestBed } from '@angular/core/testing';

import { GPTConversationService } from './gptconversation.service';

describe('GPTConversationService', () => {
  let service: GPTConversationService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GPTConversationService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
