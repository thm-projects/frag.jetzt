/*import { TestBed } from '@angular/core/testing';

import { GPTAPISettingService } from './gptapisetting.service';

describe('GPTAPISettingService', () => {
  let service: GPTAPISettingService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GPTAPISettingService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
