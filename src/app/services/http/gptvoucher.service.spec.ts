/*import { TestBed } from '@angular/core/testing';

import { GPTVoucherService } from './gptvoucher.service';

describe('GPTVoucherService', () => {
  let service: GPTVoucherService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GPTVoucherService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
