/*import { TestBed } from '@angular/core/testing';

import { DbLocalRoomSettingService } from './db-local-room-setting.service';

describe('DbLocalRoomSettingService', () => {
  let service: DbLocalRoomSettingService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DbLocalRoomSettingService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
