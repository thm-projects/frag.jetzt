/*import { TestBed } from '@angular/core/testing';

import { DbReadMotdService } from './db-read-motd.service';

describe('DbReadMotdService', () => {
  let service: DbReadMotdService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DbReadMotdService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
