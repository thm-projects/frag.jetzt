/* import { TestBed } from '@angular/core/testing';

import { DbMotdService } from './db-motd.service';

describe('DbMotdService', () => {
  let service: DbMotdService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DbMotdService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
