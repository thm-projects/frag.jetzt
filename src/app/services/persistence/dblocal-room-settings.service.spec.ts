/*import { TestBed } from '@angular/core/testing';

import { DBLocalRoomSettingsService } from './dblocal-room-settings.service';

describe('DBLocalRoomSettingsService', () => {
  let service: DBLocalRoomSettingsService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DBLocalRoomSettingsService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/
