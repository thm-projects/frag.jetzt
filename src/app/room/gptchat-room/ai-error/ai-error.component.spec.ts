/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AiErrorComponent } from './ai-error.component';

describe('AiErrorComponent', () => {
  let component: AiErrorComponent;
  let fixture: ComponentFixture<AiErrorComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AiErrorComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AiErrorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
